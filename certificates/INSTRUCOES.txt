⚠️ ATENÇÃO - CERTIFICADOS AINDA FALTANDO

Os campos Certificate e Private Key estão vazios no n8n.

Você precisa:

1. No n8n, procure pela credencial "SSL Certificates account 3"
   (essa é usada no OAuth e pode ter os certificados)

2. Se encontrar, copie:
   - Certificate → salve como cert.pem
   - Private Key → salve como key.pem

3. Se o campo CA tiver conteúdo (não a passphrase), copie e salve como ca.pem

4. A passphrase já está configurada: CeciM@042425

Após adicionar cert.pem e key.pem, reinicie o servidor.

